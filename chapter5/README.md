# 5章

## 40 
APIとは、アプリケーション同士が機能やデータをやり取りするためのインターフェースである。特にREST APIは、HTTPを利用してリソースをやり取りする設計思想であり、URLでリソースを指定し、GET・POST・PUT・DELETEなどのメソッドで操作を行う特徴をもつ。RESTはステートレスであり、サーバー側はクライアントの状態を保持しない。そのため処理が単純化され、拡張性とスケーラビリティが高いAPI設計として広く利用されている。
